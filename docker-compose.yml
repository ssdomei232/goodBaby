services:
  goodbaby:
    image: mei232/goodbaby:v0.1.6
    ports:
      - "8088:8088"
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    networks:
      - app-network
    volumes:
      - ./config.json:/app/config.json:ro
      - ./tmp:/app/tmp
    environment:
      - GIN_MODE=release

networks:
  app-network:
    driver: bridge